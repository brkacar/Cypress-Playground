{
  "name": "cypres-playground",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cypress:smoke-firefox": "npx cypress run --spec 'cypress/e2e/WebElements.cy.js' --browser firefox",
    "cypress:smoke-chrome": "npx cypress run --spec 'cypress/e2e/WebElements.cy.js' --browser chrome",
    "cypress:smoke-all": "npm-run-all cypress:smoke-firefox cypress:smoke-chrome"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^15.9.0"
  },
  "dependencies": {
    "cypress-iframe": "^1.0.1",
    "npm-run-all": "^4.1.5"
  }
}
